get:
  tags:
    - Addresses
  summary: List addresses
  description: List known Stargate addresses.
  operationId: listAddresses
  responses:
    '200':
      description: OK.
      content:
        application/json:
          schema:
            $ref: ../components/schemas/AddressCollection.yaml

