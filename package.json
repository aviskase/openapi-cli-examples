{
  "name": "stargate-network",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.29"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build:json": "openapi bundle -o dist/openapi --ext yaml",
    "build:yaml": "openapi bundle -o dist/openapi --ext json",
    "build": "npm run build:json && npm run build:yaml",
    "test": "openapi lint"
  }
}
